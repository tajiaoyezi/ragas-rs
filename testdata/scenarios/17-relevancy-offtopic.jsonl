{"sample_type":"single_turn","user_input":"How long are automated daily backups retained on the Standard plan?","response":"Backups are an important part of any disaster-recovery strategy, and we strongly recommend testing your restore process regularly. I'm not certain of the exact figure here.","retrieved_contexts":["Backup retention by plan: automated daily backups are retained for 7 days on the Standard plan and 30 days on the Business plan. Backups older than the retention window are automatically deleted."],"reference":"On the Standard plan, automated daily backups are retained for 7 days, after which they are automatically deleted.","metadata":{"note":"evasive: generic backup advice plus 'I'm not certain', never states the 7-day retention; low relevancy"}}
{"sample_type":"single_turn","user_input":"What port does the managed Postgres instance listen on by default?","response":"You can change the listening port at any time under the instance connection settings if the default conflicts with a firewall rule.","retrieved_contexts":["Networking defaults: each managed Postgres instance listens on TCP port 5432 by default. The listening port can be changed under the instance connection settings."],"reference":"The managed Postgres instance listens on TCP port 5432 by default, which you can change in the connection settings.","metadata":{"note":"off-topic drift: answers how to CHANGE the port, never states the default 5432 that was asked; low relevancy"}}
{"sample_type":"single_turn","user_input":"How do I rotate the database password for a service account?","response":"That's a good question. Password management can be tricky, and policies vary between teams, so I'd suggest checking with whoever set up your account.","retrieved_contexts":["Rotating credentials: in the Credentials tab, click Rotate next to a service account and confirm. A new password is generated immediately and the previous password is revoked at once."],"reference":"To rotate the database password for a service account, open the Credentials tab, click Rotate next to the service account, and confirm; a new password is generated immediately and the old one is revoked.","metadata":{"note":"evasive: noncommittal deflection, gives no rotation steps; low relevancy"}}
{"sample_type":"single_turn","user_input":"What is the maximum number of read replicas allowed per primary instance?","response":"To go beyond the standard limit you would enable the high-fan-out option by contacting support, which is handled on a case-by-case basis.","retrieved_contexts":["Read replica limits: each primary instance supports up to 5 read replicas. Going beyond 5 replicas requires enabling the high-fan-out option through support."],"reference":"You can attach up to 5 read replicas per primary instance; to go beyond 5 you must contact support to enable the high-fan-out option.","metadata":{"note":"off-topic drift: answers how to EXCEED the limit, never states the maximum of 5 that was asked; low relevancy"}}
{"sample_type":"single_turn","user_input":"Which TLS versions are accepted for client connections?","response":"Security is a top priority for us and we continuously review our encryption posture, but I'm not sure I can give you the specifics on that right now.","retrieved_contexts":["Transport security: client connections accept TLS 1.2 and TLS 1.3 only. TLS 1.0 and 1.1 are rejected and plaintext (unencrypted) connections are refused."],"reference":"Client connections accept TLS 1.2 and TLS 1.3 only; TLS 1.0 and 1.1 are rejected, and unencrypted connections are refused.","metadata":{"note":"evasive: generic security boilerplate plus 'I'm not sure', never names TLS 1.2/1.3; low relevancy"}}
