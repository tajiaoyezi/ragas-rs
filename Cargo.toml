[package]
name = "ragas"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Rust core for embeddable RAG/LLM evaluation."

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
